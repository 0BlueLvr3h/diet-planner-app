# Dipendenze (si reinstallano con npm install)
node_modules/
server/node_modules/

# Build del frontend (si rigenera con npm run build)
dist/

# Database: dati sul server, NON su GitHub
server/*.db
server/*.db-shm
server/*.db-wal

# Log e file di sistema
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Variabili d'ambiente / segreti
.env
.env.*

# Chiavi SSH e file sensibili (non devono MAI finire su GitHub)
*.key
*.pem